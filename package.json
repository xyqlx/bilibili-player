{
  "name": "bilibili-player",
  "version": "1.0.0",
  "description": "play bilibili video in background",
  "main": "index.js",
  "scripts": {
    "compile": "tsc --incremental -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc --incremental -p tsconfig.json && copy *.json out && node out\\index.js",
    "quickstart": "node out\\index.js"
  },
  "author": "xyqlx",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11.7",
    "tslib": "^2.3.1",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "command-line-args": "^5.2.0",
    "playwright": "^1.16.3"
  }
}
